<!doctype html>
<html lang="fr">
	<head>
		<meta http-equiv="Content-Type" content="text/html; charset=utf-8">
		<meta name="viewport" content="width=device-width">
		<title>Campagne de vote : {{ campaign.name }}</title>
    </head>
    <body>
        <p>Pour participer à la campagne {{ campaign.name }} :</p>
        <a href="{{ url('voter_welcome', {'uuid': voter.uuid}) }}">Cliquez sur ce lien</a>
    </body>
</html>