{% extends '/dashboard/base.html.twig' %}

{% block body %}
    <section class="voters_list">
        <h1>
            Participants au vote :&ensp; 
                        
            {{ campaign.name }}
        </h1>
        {{ form_start(form) }}
        {{ form_row(form.file) }}
        <button class="btn btn-primary mb-3">
            <i class="bi bi-upload"></i>
        </button>
        <hr>
            Exemple de fichier à remplir
        </hr>
        <div>
            <img src="{{ asset('build/images/screencsv.jpeg') }}" alt="brand" class="figure-img img-fluid rounded" />
        </div>
        <div>
            <a class="btn btn-primary my-5 d-md-block col-4 mx-auto justify-content-md-center" href="{{ path('campaign_edit', {'uuid': campaign.uuid}) }}">
                Retour à la page d'admin
                            
            </a>
        </div>
        {{ form_end(form) }}
    {% endblock %}
    