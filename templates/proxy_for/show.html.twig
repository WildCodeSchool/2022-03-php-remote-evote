{% extends 'base.html.twig' %}

{% block title %}ProxyFor{% endblock %}

{% block body %}
    <h1>ProxyFor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ proxy_for.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ proxy_for.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_proxy_for_index') }}">back to list</a>

    <a href="{{ path('app_proxy_for_edit', {'id': proxy_for.id}) }}">edit</a>

    {{ include('proxy_for/_delete_form.html.twig') }}
{% endblock %}
